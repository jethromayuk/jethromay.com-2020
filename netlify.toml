[build]
command = "hugo --minify"
publish = "public"

[build.environment]
HUGO_VERSION = "0.80.0"
HUGO_ENV = "production"

[[plugins]]
  package = "netlify-plugin-hugo-cache-resources"

	[plugins.inputs]
	debug = true
